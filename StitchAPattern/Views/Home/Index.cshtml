@model IEnumerable<StitchAPattern.StitchingModels.BaseStitch>

@{
    ViewBag.Title = "Home Page";
}

@foreach (var item in Model)
{
    <div>
        <h4>@item.Name</h4>
        <h4>@item.Body</h4>
        <div>
            @item.Pattern
            </div>
    </div>
}
